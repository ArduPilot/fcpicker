{
  "name": "fcpicker",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "ensure-deps": "[ -d frontend/node_modules ] || npm --prefix frontend ci --no-audit --no-fund",
    "build": "npm run ensure-deps && npm --prefix frontend run build",
    "build:spa": "npm run ensure-deps && npm --prefix frontend run build:spa",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm run ensure-deps && npm --prefix frontend run lint",
    "deploy": "wrangler deploy",
    "prepare": "git config core.hooksPath .githooks || true",
    "test": "npm --prefix frontend run test",
    "test:e2e": "npm --prefix frontend run test:e2e",
    "test:py": ".venv/bin/pytest",
    "test:all": "npm run test:py && npm run test && npm run test:e2e"
  }
}
